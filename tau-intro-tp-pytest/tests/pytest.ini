[pytest]
markers =
    accumulator: marker for accumulator function tests
    acme_bank
    api: marker for api function tests
    duckduckgo: marker for duckduckgo function tests
    math: marker for math function tests
    ui: marker for ui function tests
testpaths = tests

